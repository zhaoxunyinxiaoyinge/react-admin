import  State from "./state"

export default (pravite=State,action)=>{
    let newData=JSON.parse(JSON.stringify(pravite));
    switch (action.type) {
        default:
        return State
    }
}



